chore: setup pipeline deploy automatisé

- Ajout scripts/deploy/ (build-deploy.sh, config.sh)
- Fix Dockerfile (suppression client.html inexistant)
- Fix docker-compose.yml (port dynamique via .env)
- Fix styles.css (fond sombre panels sessions/db/subscriptions/tokens)
- Fix .env (suppression doublon USER_HASH_SECRET)
